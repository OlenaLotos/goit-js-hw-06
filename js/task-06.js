const input = document.querySelector('#validation-input');

input.addEventListener('blur', onInputBlur);

console.log(input);
function onInputBlur() { 
    if (input.value.length = input.data-length) {
        .valid; 
    } else {
        .invalid;
};

не зроблене ще